// Edit this to point to your deployed backend chat endpoint.
// Example: http://localhost:3001/api/chat
// If left blank or null, the UI will use mock responses for testing.

const CONFIG = {
  //API_URL: "https://jawadahmadcs.pythonanywhere.com/api/ask", // <- default backend endpoint
  API_URL: "http://127.0.0.1:8000/api/ask",
  MOCK_MODE: false
};